#include <stdio.h>
int main() {
    int i,n,a,b;
    for (i=0 ; i<n ; i=i++); {
        scanf("%d",&a);
        if (a=-99) {
            printf("%d",a); 
        }
        else {
            b=a+b;
            a=b; 
        }

    }
}