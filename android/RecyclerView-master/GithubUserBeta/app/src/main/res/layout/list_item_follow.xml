<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="16dp">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/following_img_avatar"
        android:layout_width="55dp"
        android:layout_height="55dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@android:color/darker_gray" />

    <TextView
        android:id="@+id/following_tv_username"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/username"
        android:textSize="16sp"
        android:textStyle="bold"
        android:layout_marginStart="65dp"
        android:layout_marginTop="7dp"
        app:layout_constraintStart_toEndOf="@+id/following_img_avatar"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/following_tv_htmlUrl"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/html_url"
        android:ellipsize="end"
        android:maxLines="2"
        android:layout_marginStart="65dp"
        android:layout_marginTop="7dp"
        app:layout_constraintStart_toEndOf="@+id/following_img_avatar"
        app:layout_constraintTop_toBottomOf="@+id/following_tv_username" />

</androidx.constraintlayout.widget.ConstraintLayout>