#include <stdio.h>
int main() {
	int count,a;
	scanf("%d",&count);
	for (int i = 0; i < count; ++i)
	{
		for (int k = 0; k < count; ++k)
		{
			a;
			for (int j = 0; j <= a; ++j)
			{
				printf(" ");
			}
			printf("*");
		}
		printf("\n");
	}
}