#include <stdio.h>
int main() {
	int n,a;
	scanf("%d",&n);
	if (n==1) printf("*\n");
	else {
		for (int i = 0; i < (2*n)-1; ++i)
		{
			printf("*");
		}
		printf("\n");
		for (int i = 0; i < k; ++i)
		{
			/* code */
		}
	}
}