#include <stdio.h>
int main() {
	long int a,b,sum1;
	scanf("%ld",&a);
	b=0;
	c=0;
	if (a=-99) {
		print("0 0");
	}
	while(a!=-99) {
		scanf("%ld",&a);
		b=0;
		if (sum1>a) sum1=a; {
			b=++;}
		else (sum1<=a) sum1=a; {
			c=--;
		}
	printf("%ld %ld\n", b,c);
	}

}